<!doctype html>
<html lang="en">
  <%- include("../partials/header"); %>
  <body>
    <section class="w-[100dvw] h-[100dvh] flex flex-col justify-center items-center">
        <% for(const booking of bookings){%>
            <div>
                <p><%=booking.customerName%></p>
            </div>
        <%}%>
    </section>
  </body>
</html>
